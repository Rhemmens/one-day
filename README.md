# One Day

Beta frontend for One Day coaching app.

## Deploy

Import this repository into Vercel. The root `index.html` is a static beta build.

## Backend

Supabase project ref: `nnzlaczbomzyfmpkyllo`

Do not commit Supabase secret/service-role keys to this repository. Only browser-safe publishable keys belong in frontend configuration.
