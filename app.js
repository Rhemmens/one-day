let role=null,view='today',checked=[false,false,false,false],weights={hip:'',split:'',kick:''},messages=[['Maddie','Nice work yesterday. Keep today controlled.']];
const root=()=>document.getElementById('root');
function shell(content,coach=false){return `<div class="app"><header class="top"><div><p class="eyebrow">ONE DAY</p><h2 style="margin:4px 0 0">${coach?'Coach notebook':'Hi, Maddie'}</h2></div><button class="btn" onclick="role=null;render()">Log out</button></header><main class="content">${content}</main><nav class="nav ${coach?'coach':'client'}">${coach?`<button class="${view==='clients'?'active':''}" onclick="view='clients';render()">Clients</button><button class="${view==='build'?'active':''}" onclick="view='build';render()">Build</button><button class="${view==='messages'?'active':''}" onclick="view='messages';render()">Messages</button><button class="${view==='billing'?'active':''}" onclick="view='billing';render()">Billing</button><button class="${view==='library'?'active':''}" onclick="view='library';render()">Library</button>`:`<button class="${view==='today'?'active':''}" onclick="view='today';render()">Today</button><button class="${view==='calendar'?'active':''}" onclick="view='calendar';render()">Calendar</button><button class="${view==='messages'?'active':''}" onclick="view='messages';render()">Messages</button><button class="${view==='profile'?'active':''}" onclick="view='profile';render()">Profile</button>`}</nav></div>`}
function render(){if(!role){root().innerHTML=`<div class="login"><div class="box"><div class="logo">1</div><p class="eyebrow" style="margin-top:14px">ONE DAY</p><h1>Today is enough.</h1><p class="muted">Training, habits, progress and coaching — one day at a time.</p><div class="money"><p class="eyebrow">BETA</p><p class="muted">Explore the real client and coach flows below.</p><button class="btn primary full" onclick="role='client';view='today';render()">Open client beta</button><button class="btn full" style="margin-top:9px" onclick="role='coach';view='clients';render()">Open coach beta</button></div><p class="small">Real account authentication and Supabase persistence are the next connection step.</p></div></div>`;return} root().innerHTML=shell(role==='coach'?coachHTML():clientHTML(),role==='coach')}
function clientHTML(){if(view==='calendar')return calendarHTML();if(view==='messages')return messagesHTML(false);if(view==='profile')return profileHTML();if(view==='workout')return workoutHTML();if(view==='complete')return completeHTML();const done=checked.filter(Boolean).length;return `<div class="todayDate">Sunday,<br>August 16</div><p class="motto">You create the life you live.</p><div class="note"><p class="eyebrow">FROM YOUR COACH</p><p style="font-family:Georgia,serif">Move with control today. Quality reps &gt; rushing.</p></div><div class="row"><div class="grow"><p class="eyebrow">TODAY</p><h2 style="margin:4px 0">Your list</h2></div><b style="color:var(--gold)">${done}/4</b></div><div class="progress"><span style="width:${done/4*100}%"></span></div><button class="todo" onclick="view='workout';render()"><span class="check ${checked[0]?'done':''}"></span><span class="grow"><b>Glute Day</b><span class="small" style="display:block;margin-top:4px">3 exercises · tap to open workout</span></span><span>›</span></button>${['8,000 steps','Have a 30g protein meal','Drink 2 Hydros'].map((x,i)=>`<button class="todo" onclick="checked[${i+1}]=!checked[${i+1}];render()"><span class="check ${checked[i+1]?'done':''}"></span><b class="grow">${x}</b></button>`).join('')}<button class="btn primary full" style="margin-top:22px" onclick="view='complete';render()">Complete day</button>`}
function workoutHTML(){return `<button class="btn" onclick="view='today';render()">← Back to today</button><p class="eyebrow" style="margin-top:18px">TODAY'S WORKOUT</p><h1>Glute Day</h1><div class="money"><b>Ready to train?</b><p class="muted">Log your weight and notes as you go.</p></div>${exercise('Barbell Hip Thrust','4 × 8',145,'hip')}${exercise('Bulgarian Split Squat','3 × 10/side',30,'split')}${exercise('Cable Kickback','3 × 12/side',25,'kick')}<button class="btn primary full" onclick="checked[0]=true;view='today';render()">Save workout</button>`}
function exercise(name,rx,last,key){const v=Number(weights[key]||0),win=v>last;return `<div class="work"><h3 style="margin:0">${name}</h3><p class="small">${rx}</p><div class="target ${win?'win':''}">${win?`New best — ${v} lb · +${v-last} lb PR`:`Last time ${last} lb · beat it`}</div><div class="weight"><span class="small">Weight used</span><div><input type="number" value="${weights[key]}" oninput="weights['${key}']=this.value"> lb</div></div><div class="stack" style="margin-top:12px"><textarea placeholder="Notes for Maddie"></textarea></div></div>`}
function calendarHTML(){return `<p class="eyebrow">CALENDAR</p><h1>Your plan</h1><p class="muted">Review completed days and see what's coming next.</p><div class="calendar">${['S','M','T','W','T','F','S'].map(x=>`<b style="text-align:center;font-size:10px">${x}</b>`).join('')}${Array.from({length:31},(_,i)=>`<button class="${i===15?'today':''}">${i+1}</button>`).join('')}</div><div class="note"><p class="eyebrow">AUGUST 16</p><b>Glute Day + 3 habits</b><p class="small">Today</p></div>`}
function messagesHTML(coach){return `<p class="eyebrow">MESSAGES</p><h1>${coach?'Client messages':'Maddie'}</h1><div class="card">${messages.map((m,i)=>`<div class="message ${i%2?'me':''}"><b>${m[0]}</b><br>${m[1]}</div>`).join('')}<div class="stack" style="margin-top:14px"><input id="msg" placeholder="Write a message"><button class="btn primary" onclick="sendMessage('${coach?'Coach':'You'}')">Send</button></div></div>`}
window.sendMessage=(name)=>{const e=document.getElementById('msg');if(!e||!e.value.trim())return;messages.push([name,e.value.trim()]);render()}
function profileHTML(){return `<p class="eyebrow">PROFILE</p><h1>Maddie</h1><div class="card"><b>Your coach</b><p class="muted">Maddie · MaddieBelleFit</p></div><div class="note"><p class="eyebrow">COACH NOTES</p><b>Keep progressing load slowly.</b><p class="small">Focus on controlled reps and staying consistent this week.</p></div><div class="card"><b>Billing</b><p class="small">Plan and payment history will live here.</p></div>`}
function completeHTML(){return `<div class="success"><div class="successMark">✓</div><p class="eyebrow">DAY COMPLETE</p><h1>Way to kill the day.</h1><p class="muted">Send Maddie anything she should know before you wrap up.</p><div class="stack" style="text-align:left"><textarea placeholder="Questions, soreness, wins, concerns, energy, anything else..."></textarea><button class="btn primary full" onclick="checked=[true,true,true,true];view='today';render()">Submit check-in</button></div></div>`}
function coachHTML(){if(view==='messages')return messagesHTML(true);if(view==='build')return buildHTML();if(view==='billing')return billingHTML();if(view==='library')return libraryHTML();return `<div class="row"><div class="grow"><p class="eyebrow">SUNDAY · AUGUST 16</p><h1>Your clients</h1></div><button class="btn primary" onclick="alert('Client email invitations connect here.')">+ Add client</button></div>${[['Reece','HYROX + consistency','3/5'],['Olivia','Glutes + nutrition','5/5'],['Sofia','Build confidence','2/6']].map(x=>`<div class="card row"><div class="avatar">${x[0][0]}</div><div class="grow"><b>${x[0]}</b><p class="small">${x[1]}</p></div><b style="color:var(--gold)">${x[2]}</b><button class="btn" onclick="view='build';render()">Open</button></div>`).join('')}<div class="money"><p class="eyebrow">ACTIVITY</p><b>Reece logged a new PR</b><p class="small">Barbell Hip Thrust · 155 lb</p></div>`}
function buildHTML(){return `<button class="btn" onclick="view='clients';render()">← Clients</button><p class="eyebrow" style="margin-top:18px">BUILD FOR REECE</p><h1>Plan the day</h1><div class="card"><b>Workout</b><p class="small">Glute Day</p><div class="work"><b>Barbell Hip Thrust</b><p class="small">4 × 8 · Last 145 lb</p></div><div class="work"><b>Bulgarian Split Squat</b><p class="small">3 × 10/side · Last 30 lb</p></div><button class="btn full">+ Add exercise</button></div><div class="card"><b>Habits</b><p class="small">8,000 steps · 30g protein meal · Drink 2 Hydros</p><button class="btn full">+ Add habit</button></div><div class="stack"><textarea placeholder="Coach note for today"></textarea><button class="btn primary full" onclick="alert('Day saved')">Save day</button></div>`}
function billingHTML(){return `<p class="eyebrow">BILLING</p><h1>Client plans</h1><div class="money"><span class="small">Monthly recurring</span><h2>$850</h2><span class="small">3 active clients</span></div>${[['Reece','Active','$300'],['Olivia','Active','$300'],['Sofia','Trial','$250']].map(x=>`<div class="card row"><div class="grow"><b>${x[0]}</b><p class="small">${x[1]}</p></div><b>${x[2]}</b></div>`).join('')}`}
function libraryHTML(){return `<p class="eyebrow">LIBRARY</p><h1>Reusable coaching tools</h1><div class="tabs"><button class="btn primary">Exercises</button><button class="btn">Habits</button></div>${['Barbell Hip Thrust · Glutes · 4 × 8','Bulgarian Split Squat · Glutes · 3 × 10','Cable Kickback · Glutes · 3 × 12'].map(x=>`<div class="card"><b>${x}</b></div>`).join('')}<button class="btn primary full">+ Add exercise</button><div style="height:16px"></div>${['8,000 steps','30g protein meal','Drink 2 Hydros','Take progress pictures'].map(x=>`<div class="card"><b>${x}</b><p class="small">Reusable habit</p></div>`).join('')}`}
render();
